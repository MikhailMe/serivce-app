<?xml version="1.0" encoding="utf-8"?>
<manifest package="mishas.clientofapp"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.StartingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpPart1Activity"/>
        <activity android:name=".activities.SignUpPart2Activity"/>
        <activity
            android:name=".activities.MainScreenActivity"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activities.BagActivity"/>
        <activity android:name=".activities.FoodActivity"/>
        <activity android:name=".activities.LiquidActivity"/>
        <activity android:name=".activities.PaymentActivity"/>
        <activity android:name=".activities.RecoveryPasswordActivity"/>
        <activity android:name=".activities.ChoiceShopActivity"/>
    </application>

</manifest>